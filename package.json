{
  "name": "dsh-output-styles",
  "description": "Persistent output styles for DeepSeek Harness Web: choose a built-in style or create named styles from the Settings page",
  "version": "0.1.0",
  "type": "module",
  "license": "MIT",
  "main": "lib/index.js",
  "exports": {
    ".": "./lib/index.js",
    "./client": "./lib/client.js",
    "./typert": "./lib/remote.js",
    "./package.json": "./package.json"
  },
  "dsh": {
    "client": {
      "inject": [
        "@deepseek-ai/dsh-client-runtime"
      ],
      "platform": "web"
    }
  },
  "files": [
    "lib/",
    "dynamic/",
    "scripts/",
    "cordis.patch.example.yml",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "bundle:dynamic": "node scripts/bundle-dynamic.mjs",
    "test": "node --check lib/client.js && node --input-type=module --check < lib/index.js && node --input-type=module --check < lib/remote.js && node --input-type=module --check < lib/catalog.js && node --test"
  },
  "peerDependencies": {
    "@deepseek-ai/cordis": "^4.0.1",
    "@deepseek-ai/dsh-typert-protocol": "^0.1.0-rc.7",
    "@deepseek-ai/schemastery": "^3.18.1",
    "react": "^18.2.0"
  }
}
